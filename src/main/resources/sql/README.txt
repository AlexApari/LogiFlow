Coloca aquí tus scripts SQL de inicialización, migración o respaldo de la base de datos para el proyecto LogiFlow.

Ejemplo de uso:
- init.sql
- schema.sql
- data.sql

Esta carpeta es solo para referencia y no se expone públicamente en producción.